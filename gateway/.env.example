PORT=4000
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Secrets used to sign JWTs and sessions – change these in each environment
JWT_SECRET=your-jwt-secret
SESSION_SECRET=your-session-secret

# Where the frontend runs locally
# Postgres connection string for Docker Compose network
FRONTEND_ORIGIN=http://localhost:5173

DATABASE_URL=postgresql://postgres:postgres@postgres:5432/gateway_db
