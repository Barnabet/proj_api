PORT=4000
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Secrets used to sign JWTs and sessions – change these in each environment
JWT_SECRET=your-jwt-secret
SESSION_SECRET=your-session-secret

# Where the frontend runs locally
FRONTEND_ORIGIN=http://localhost:5173

# Local Postgres connection string (adjust if you use a different user/DB)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/gateway_db
